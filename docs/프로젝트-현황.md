# 일정공방 — 프로젝트 현황 (컨텍스트 관리용)

> 최종 업데이트: 2026-07-14 / 위젯 릴리스: `v0.1.4` (**v0.1.5 릴리스 대기 중 — 아래 참고**)
>
> **다음에 할 일 (순서대로)**
> 1. **위젯 v0.1.5 릴리스** — Win+D 수정이 코드엔 있으나 **아직 배포 안 됨**.
>    서명 키(`~/.tauri/`)가 있는 PC 에서만 가능. 절차: `docs/위젯-설계.md` §11
> 2. **카카오톡으로 조직 초대(초대 링크)** → `docs/조직-워크스페이스.md` §10-(1)
> 3. **위젯에 조직 일정 반영** → §10-(2)
> 4. 안드로이드 실기기 확인 (폰만 연결하면 됨) → `docs/안드로이드-앱.md` §5
>
> 이 문서는 작업 재개 시 맥락을 빠르게 잡기 위한 요약본입니다.

---

## 1. 프로젝트 개요

**일정공방** — 요일별 시간 블록으로 주간 계획을 짜고, 상황별(방학·학기중·휴가) **프리셋**으로 저장해
여러 기기에서 한눈에 보는 앱. "지금 할 일"을 자동 하이라이트.
바탕화면에 띄워두는 **데스크탑 위젯**으로도 본다.

- **GitHub**: https://github.com/enryu0210/Schedule_app.git (branch: `main`)
- **웹 배포**: Vercel (Root Directory = `web`) → `schedule-repo-eight.vercel.app` (main 푸시 시 자동 배포)
- **위젯 배포**: GitHub Releases (`widget-v*` 태그) + 앱 내 자동 업데이트
- **디자인 톤**: 테라코타/크림 따뜻한 톤

---

## 2. 기술 스택

| 영역 | 스택 | 상태 |
|------|------|------|
| 웹 | React 18 + Vite + TypeScript | ✅ |
| 배포 | Vercel (자동 배포: main 푸시 시) | ✅ |
| DB | Supabase (Postgres + RLS + Realtime) | ✅ |
| 인증 | 카카오 소셜 로그인 (Supabase Auth) | ✅ |
| 데스크탑 위젯 | Tauri 2 (Rust + WebView2) — 윈도우 | ✅ |
| 조직 워크스페이스 | Supabase 테이블 4개 + RLS (웹) | 🔨 1차 구현 완료, 실사용 검증 전 |
| 안드로이드 앱 | Capacitor 7 (웹뷰 셸, `mobile/`) | 🔨 1단계 — APK 빌드 성공, 실기기 확인 전 |
| 안드로이드 홈 위젯 | Kotlin + Glance (예정) | ⬜ 2단계 |

> ⚠️ liteLLM 라이브러리 사용 금지(보안). AI 기능은 직접 SDK 사용 예정.

---

## 3. 진행 완료 (Done)

### 1~3단계 — 웹 MVP / 클라우드 / 테마 ✅
- 요일 탭 + 시간 블록 시간표, 블록 추가·편집·삭제, 현재 시각 하이라이트
- Supabase `user_data` 테이블에 사용자별 프리셋 통짜 jsonb 저장 + RLS(`auth.uid()`)
- 카카오 로그인, 기기 간 동기화, 로그인 게이트, 따뜻한 톤 테마, 프리셋 삭제

### 4단계 — 보기 방식 / 편집 UX ✅
- 차트형 ↔ 그래프형(에브리타임식) 시간표 토글
- 그래프 뷰에서 블록 **드래그 이동 / 리사이즈**

### 5단계 — 데스크탑 위젯 ✅ (`desktop/`, 상세는 `docs/위젯-설계.md`)
- Tauri 셸이 **배포된 웹앱을 `?widget=1` 로 로드** — 로그인·동기화 코드를 두 벌로 관리하지 않는다
- 프레임리스/투명 창, **Win+D(바탕화면 보기)를 눌러도 안 내려감** (Win32 서브클래싱)
- **트레이 아이콘** — 보이기·숨기기 / 항상 위 / 윈도우 시작 시 실행 / 업데이트 확인 / 종료
- 창 위치·크기 기억, 시작 프로그램 등록 (공식 플러그인)
- **위젯 안에서 카카오 로그인** — 로그인 동안만 창을 키웠다가 되돌린다
- **자동 업데이트** (`tauri-plugin-updater`) — 서명 검증 후 설치·재시작
- 웹 상단바에 **다운로드 버튼** → GitHub Releases 고정 링크
- **오프라인 캐시** — 마지막으로 읽은 일정을 localStorage 에 남겨, 인터넷이 끊겨도 계속 보여준다.
  이때 헤더에 "오프라인 · HH:MM 기준" 배지를 띄워 옛날 일정임을 알린다 (`lib/widgetCache.ts`)
- **Win+D 수정 (v0.1.5 예정, 코드는 커밋됨)** — '항상 위'를 꺼두면 Win+D 때 위젯이 사라지던 문제.
  측정해보니 최소화도 이동도 아니었고 **바탕화면(Progman)이 위를 덮은 것**이었다.
  바탕화면이 포그라운드가 되는 순간(`SetWinEventHook`)에 위젯을 **TOPMOST 로 올렸다 곧바로 NOTOPMOST 로 내린다**
  → '항상 위'로 고정하지 않으면서 바탕화면 위에 남는다. (`HWND_TOP` 만으로는 안 된다 — 실측)

### 6단계 — 조직(팀/기업) 워크스페이스 🔨 (상세: `docs/조직-워크스페이스.md`)
- 조직 생성 / **8자리 초대 코드**로 참여 (카카오는 이메일이 없을 수 있어 이메일 초대 불가)
- 팀원이 프리셋을 **조직에 공유**(사본) → 개인 계획표는 여전히 비공개
- **팀 빈 시간 찾기** — 30분 칸마다 바쁜 인원을 세어 '모두 가능'한 시간을 초록으로
- 관리자가 **조직 시간표를 조직 페이지에서 직접 편집** → 고치는 즉시 자동 배포
- 공유 사본 **자동 갱신** — 개인 프리셋을 고치면 공유본도 따라간다

---

## 4. 현재 구조 (파일 지도)

```
schedule_app/
├─ assets/                     # 원본 시안 + 앱 아이콘
├─ supabase/
│  ├─ schema.sql               # 개인용: user_data + RLS + Realtime 발행
│  └─ org-schema.sql           # ★ 조직용: 테이블 4개 + RLS + create_org/join_org RPC
├─ docs/
│  ├─ 2단계-Supabase-카카오-설정.md
│  ├─ 위젯-설계.md              # ★ 위젯 설계·릴리스 절차·함정
│  ├─ 조직-워크스페이스.md       # ★ 조직 기능 설계·함정·다음 작업
│  ├─ 안드로이드-앱.md
│  ├─ AI-시간표-비용-산정.md
│  └─ 프로젝트-현황.md          # (이 문서)
├─ desktop/                    # ★ 데스크탑 위젯 (Tauri)
│  └─ src-tauri/src/
│     ├─ lib.rs                # 부팅 + 플러그인(위치기억/자동실행/업데이터) 등록
│     ├─ tray.rs               # 트레이 아이콘 + 메뉴
│     ├─ update.rs             # 자동 업데이트 (확인 → 물어보고 설치 → 재시작)
│     ├─ win_desktop.rs        # Win+D 대응 (메시지 가로채기 + 바탕화면 감시 훅)
│     └─ win_webview.rs        # 웹뷰 우클릭 메뉴·브라우저 단축키 끄기
├─ mobile/                     # 안드로이드 앱 (Capacitor)
│  └─ scripts/                 # android-env.ps1(경로 자동 탐색) / install-debug.ps1
└─ web/
   └─ src/
      ├─ App.tsx               # 라우터: 위젯모드 → 로딩 → 로그인 → (개인 Planner | 조직 OrgWorkspace)
      ├─ types.ts              # Preset / DayPlan / ScheduleBlock + Organization / Workspace …
      ├─ styles/tokens.css     # ★ 색상 토큰(테마 수정 지점)
      ├─ hooks/
      │  ├─ useAuth.tsx        # 세션/카카오 로그인·로그아웃
      │  ├─ usePresetStore.tsx # ★ 클라우드 전용 프리셋 저장소 (읽기+쓰기)
      │  ├─ useWidgetPresets.ts# 위젯 전용 읽기 전용 로더 (캐시 → Realtime + 폴링)
      │  ├─ useOrg.tsx         # ★ 조직 상태 Context (워크스페이스 전환 포함)
      │  └─ useSharedScheduleSync.ts # 공유 사본 자동 갱신
      ├─ lib/
      │  ├─ cloudStorage.ts    # fetch(예외 던짐)/load(삼킴)/saveCloudPresets
      │  ├─ orgStorage.ts      # ★ 조직 전용 접근 계층 (실패 시 예외를 던진다)
      │  ├─ widgetCache.ts     # ★ 위젯 오프라인 캐시 (localStorage, 사용자별)
      │  ├─ widgetMode.ts      # 위젯 모드 판별 + 우클릭 차단
      │  └─ widgetWindow.ts    # 로그인 동안 창 확대 → 복원
      └─ components/
         ├─ Planner.tsx        # ★ 메인 계획표 (개인)
         ├─ OrgWorkspace.tsx   # ★ 조직 화면 (공유·빈시간·시간표·구성원)
         ├─ TeamOverlapGrid.tsx# ★ 팀 빈 시간 찾기
         ├─ OrgPlanEditor.tsx  # 조직 시간표 직접 편집 + 자동 배포
         ├─ WorkspaceSwitcher.tsx / OrgDialog.tsx
         ├─ WidgetView.tsx     # 위젯 컨테이너(로그인/로딩)
         ├─ WidgetBody.tsx     # 위젯 화면(순수 컴포넌트)
         ├─ DownloadWidget.tsx # 위젯 다운로드 버튼
         └─ (PresetSidebar, ScheduleCard, WeekGrid, BlockEditor, AuthBar …)
```

**데이터 흐름**
- 웹: `useAuth`(세션) → `usePresetStore`(클라우드 읽기/쓰기) → `Planner`
- 위젯: `useAuth` → `useWidgetPresets`(**읽기 전용**) → `WidgetBody`
  웹에서 저장하면 Supabase **Realtime** 이 위젯에 즉시 알려준다 (폴링 60초는 안전망)

---

## 5. 핵심 결정 & 함정 (중요)

- **카카오 KOE205**: 카카오 "개인 개발자로 등록" + 동의항목 전부 선택 동의 +
  Supabase Kakao provider 의 "Allow users without an email" ON 으로 해결.
- **OAuth 리다이렉트는 쿼리를 지키게 한다.** `redirectTo` 를 `window.location.origin` 으로 주면
  위젯 창에서 로그인했을 때 `?widget=1` 이 날아가 **위젯에 전체 앱 화면이 뜬다**(실제로 겪음).
  안전망으로 위젯 모드를 sessionStorage 에도 기억시킨다.
- **위젯은 절대 클라우드에 쓰지 않는다.** 읽기 전용 훅(`useWidgetPresets`)을 따로 둔 이유 —
  저장 로직을 달고 다니면 오래된 상태로 웹의 최신 수정을 덮어쓸 수 있다.
- **"데이터 없음"과 "못 읽음"은 다르다.** `loadCloudPresets` 는 오류를 삼키고 `null` 을 돌려주기 때문에,
  위젯이 이걸 그대로 쓰면 네트워크가 끊긴 순간 `presets = []` 로 덮어써져 빈 화면이 됐다.
  그래서 예외를 던지는 `fetchCloudPresets` 를 따로 두고, 위젯은 실패하면 화면을 건드리지 않는다.
- **Vercel-저장소 연결**: 한때 Vercel 이 다른 저장소를 배포해 코드가 반영 안 됨.
  Settings → Git 에서 `enryu0210/Schedule_app` 연결 확인.
- **환경변수**: `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY` (Vite 는 **빌드 시점** 주입 →
  변경 후 캐시 없이 재배포 필요). Vercel + 로컬 `web/.env` 양쪽 필요.
- **위젯 관련 함정**은 `docs/위젯-설계.md` 에 모아뒀다 (MSVC 필수 / Git Bash 링커 충돌 /
  latest.json 의 BOM / PowerShell 의 `$env:VAR=""` 는 변수 삭제).

---

## 6. 개발 환경 (새 PC 에서 시작할 때)

```powershell
# 웹
cd web; npm install; npm run dev      # http://localhost:5173 (web/.env 에 Supabase 키 필요)
cd web; npm run build                 # 타입체크 + 프로덕션 빌드

# 위젯 (Rust + MSVC 필요 — rustup 은 MSVC 를 자동으로 깔아주지 않는다)
winget install --id Rustlang.Rustup -e
winget install --id Microsoft.VisualStudio.2022.BuildTools -e --override "--wait --quiet --add Microsoft.VisualStudio.Workload.VCTools --includeRecommended"
cd desktop; npm install; npm run dev   # 위젯 창 띄우기 (반드시 PowerShell 에서 — Git Bash 는 링커가 깨짐)
```

**위젯 서명 키**: `~/.tauri/schedule-widget.key` (+ `.pass`).
저장소 밖에 있고 **잃어버리면 다시는 업데이트를 밀어넣을 수 없다. 반드시 백업.**

---

## 7. 다음 할 일 (To-Do)

### 위젯
- [x] ~~오프라인 캐시~~ ✅
- [x] ~~Win+D 에서 사라지던 문제 수정~~ ✅ (코드 완료)
- [ ] **v0.1.5 릴리스** ← Win+D 수정이 사용자에게 내려가려면 필수.
      **서명 키(`~/.tauri/`)가 있는 PC 에서만 가능** (다른 기기엔 키가 없다). 절차: `docs/위젯-설계.md` §11
- [ ] **위젯에 조직 일정 반영** → `docs/조직-워크스페이스.md` §10-(2)

### 조직 (상세: `docs/조직-워크스페이스.md`)
- [x] ~~1차: 조직 생성/초대코드 참여/시간표 공유/팀 빈 시간 찾기/조직 시간표 직접 편집~~ ✅
- [x] ~~공유 사본 자동 갱신~~ ✅
- [x] ~~Realtime 구독~~ ✅ (배포/공유가 새로고침 없이 즉시 반영)
- [ ] **카카오톡으로 조직 초대** (초대 링크 공유) → §10-(1)
      ⚠️ 링크를 아는 사람은 누구나 가입 가능해진다 → 코드 만료/재발급 또는 관리자 승인을 같이 붙일 것
- [ ] 관리자의 구성원 내보내기 / 역할 변경 UI (DB 정책은 이미 있음)
- [ ] **실사용 검증** — 팀원이 실제로 자기 시간표를 공유하는가? (이 기능의 유일한 가정)

### 안드로이드 (상세: `docs/안드로이드-앱.md`)
- [x] ~~1단계: Capacitor 앱 셸 + 카카오 로그인(커스텀탭+딥링크) + APK 빌드~~ ✅
- [x] ~~Supabase Redirect URLs 에 `ilgongbang://auth-callback` 등록~~ ✅
- [ ] **실기기 확인** — 폰 연결 후 `.\mobile\scripts\install-debug.ps1`
- [ ] 모바일 반응형 다듬기 (Planner 는 데스크탑 기준 레이아웃)
- [ ] 2단계: 홈 위젯 (Kotlin + Glance) / 3단계: 알림바 상시 표시

### 웹 백로그
- [ ] 블록 겹침/빈 시간 검증
- [ ] 반응형/모바일 다듬기, 다크모드(선택)

### 로드맵
- [ ] **AI 시간표 생성** — 자연어 → 블록 자동 생성 (직접 SDK, liteLLM 금지). API 키 필요
- [ ] **네이버 로그인** — Supabase 미지원 → Edge Function 커스텀 OAuth 필요

### 보류 (결론 남김)
- **SmartScreen "알 수 없는 게시자" 경고** — 지금은 그대로 둔다.
  - EV 인증서(연 $250~400)가 예전엔 SmartScreen 을 즉시 통과시켰지만 **그 특혜는 없어졌다.**
    지금은 돈을 내도 평판이 쌓일 때까지(수 주 + 수백 건 설치) 경고가 남는다.
  - Azure Artifact Signing(월 $9.99)은 **개인은 미국·캐나다만** 가능 → 한국 개인은 자격 미달.
  - **자동 업데이트를 붙였으므로 경고는 최초 설치 1회만 겪는다.** 배포 규모가 커지면
    Microsoft Store($19 일회성, 스토어가 서명 → 경고 없음)를 검토한다.
