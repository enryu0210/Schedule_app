# 2단계(Supabase/로그인)에서 사용할 환경변수 예시.
# 실제 값은 .env 파일에 넣고, .env 는 git 에 올리지 않습니다(.gitignore 처리됨).

# Supabase
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# 3단계(AI 시간표 생성)에서 사용할 API 키 (서버 측에서만 사용 권장)
# 주의: 프론트엔드에 직접 노출하지 말 것.
# ANTHROPIC_API_KEY=
